import 'package:flutter/material.dart';

class VideoPassView extends StatelessWidget {
  const VideoPassView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('video pass view'),
      ),
    );
  }
}
